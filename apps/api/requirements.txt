# Pinned runtime requirements for pip-based deploys (Fly.io, Render, etc).
# Keep in lockstep with pyproject.toml. Regenerate via `pip-compile` if you
# prefer fully resolved transitives; the floors below mirror pyproject.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
python-dotenv>=1.0,<2.0
google-genai>=2.2.0,<3.0
httpx>=0.27,<1.0
supabase>=2.10,<3.0
PyJWT>=2.10,<3.0
python-multipart>=0.0.20
websockets>=14,<16
structlog>=24.4,<26
tenacity>=9,<10
sse-starlette>=2.1,<3.0
langgraph>=0.2,<1.0
PyYAML>=6.0,<7.0
